#include <iostream>

using namespace std;

int main() {
  double capacity, efficiency, battery_level, distance;
  cin >> capacity >> efficiency >> battery_level;
  const double km_per_kW = capacity / 100 * efficiency;
  double now = 0;
  while (now < distance) {

  }
}
